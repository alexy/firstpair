{
  "framework": "vite",
  "buildCommand": "npm run prod:build",
  "outputDirectory": "dist-prod",
  "routes": [
    {
      "src": "^/read(?:/(.*))?$",
      "dest": "/api/reader?path=$1"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "^/(.*)$",
      "dest": "/index.html"
    }
  ]
}
